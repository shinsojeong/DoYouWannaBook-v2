import React from 'react';

const Login = () => {
    return (
        <div id="login">
            <form>
                <input type="text" />
                <input type="password" />
            </form>
        </div>
    );
};

export default Login;